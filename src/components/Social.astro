---
const { platform, username } = Astro.props;
---

<a href={`https://www.${platform}.com/${username}`}>{platform}</a>

<style>
  a {
    @apply bg-[#4c1d95] hover:bg-purple-700 no-underline text-white font-bold py-2 px-4 rounded;
  }
</style>
